# Environment variables
if [ -f "/home/linuxbrew/.linuxbrew/bin/brew" ]; then
    eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
fi
export XDG_CONFIG_HOME="$HOME/.config"
export EDITOR=nvim
export GOPATH="$HOME"
export LANG=en_US.UTF-8
# export LC_ALL=en_US.UTF-8 # Comment out to avoid warnings if locale is missing
if command -v brew &> /dev/null; then
    export PKG_CONFIG_PATH="$(brew --prefix openssl)/lib/pkgconfig"
fi

# History settings
HISTFILE="$HOME/.zsh_history"
HISTSIZE=10000
SAVEHIST=10000
setopt hist_ignore_all_dups
setopt hist_ignore_space
setopt share_history
setopt EXTENDED_GLOB

# antidote
# Source antidote from brew if available
if [ -f "/home/linuxbrew/.linuxbrew/opt/antidote/share/antidote/antidote.zsh" ]; then
    source "/home/linuxbrew/.linuxbrew/opt/antidote/share/antidote/antidote.zsh"
elif command -v brew &> /dev/null && [ -f "$(brew --prefix)/opt/antidote/share/antidote/antidote.zsh" ]; then
    source "$(brew --prefix)/opt/antidote/share/antidote/antidote.zsh"
else
    echo "antidote could not be found, please install it with brew"
    # return # Don't return, let the shell load anyway
fi

zsh_plugins=${ZDOTDIR:-$HOME}/.zsh_plugins.txt

# Check if the plugins file exists
if [ -f "$zsh_plugins" ]; then
    # If the static file doesn't exist, or it's older than the plugins file
    if [[ ! -f "${zsh_plugins}.zsh" || "${zsh_plugins}.zsh" -ot "$zsh_plugins" ]]; then
        antidote bundle < "$zsh_plugins" > "${zsh_plugins}.zsh"
    fi
    source "${zsh_plugins}.zsh"
fi

# carapace completion
export CARAPACE_BRIDGES='zsh,fish,bash'
zstyle ':completion:*' format $'\e[2;37mCompleting %d\e[m'
if command -v carapace >/dev/null 2>&1; then
  autoload -Uz compinit && compinit
  source <(carapace _carapace)
fi

# Modern CLI tools
eval "$(starship init zsh)"
eval "$(zoxide init zsh)"
eval "$(mise activate zsh)"

# fzf integration (Ctrl+R: history, Ctrl+T: files, Alt+C: cd)
if command -v fzf >/dev/null 2>&1; then
  eval "$(fzf --zsh)"
fi

# ghq + fzf integration (mirrors decors/fish-ghq behaviour)
# Override selector with GHQ_SELECTOR, pass extra flags via GHQ_SELECTOR_OPTS (space-separated)
function ghq-fzf() {
  local selector=${GHQ_SELECTOR:-fzf}
  if ! command -v ghq >/dev/null 2>&1 || ! command -v "$selector" >/dev/null 2>&1; then
    zle reset-prompt
    return 1
  fi

  local selected
  selected=$(ghq list --full-path | "$selector" \
    ${(z)GHQ_SELECTOR_OPTS} \
    --query="$LBUFFER" \
    --height=40% \
    --layout=reverse \
    --border=rounded \
    --preview='eza --tree --level=1 --icons --color=always {}' \
    --preview-window='right:40%:wrap')

  if [[ -n "$selected" ]]; then
    builtin cd "$selected"
    BUFFER=""
  fi
  zle reset-prompt
}
zle -N ghq-fzf
bindkey '^g' ghq-fzf

# Path settings
export PATH="$HOME/.deno/bin:$PATH"
