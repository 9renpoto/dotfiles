{
  "name": "chezmoi-devcontainer",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "vscode",
  "postCreateCommand": "bash .devcontainer/scripts/post-create.sh",
  "postStartCommand": "echo 'Run ./devcontainer-setup.sh from your host to apply dotfiles.'",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "openai.chatgpt"
      ]
    }
  }
}
